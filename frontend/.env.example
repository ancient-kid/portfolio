# Local development backend URL (used by browser-side fetch calls)
NEXT_PUBLIC_API_URL=http://127.0.0.1:5000

# Production options:
# 1) Same-domain reverse proxy (/api/* -> backend): leave NEXT_PUBLIC_API_URL empty
# 2) Separate API host: NEXT_PUBLIC_API_URL=https://anrg.dev
